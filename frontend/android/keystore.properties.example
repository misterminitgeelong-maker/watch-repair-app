# Copy to keystore.properties next to this file (never commit).
# storeFile is relative to the android/ directory (same folder as this file).
storeFile=upload.jks
storePassword=your-store-password
keyAlias=upload
keyPassword=your-key-password
