fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlmodel==0.0.37
alembic==1.13.2
# PyJWT replaces python-jose, whose ecdsa dependency has an unfixed advisory
# (Minerva timing attack). Tokens are HS256, so no crypto extra is needed.
PyJWT>=2.10,<3
bcrypt>=4.0.0
pydantic-settings==2.5.2
psycopg[binary]>=3.3,<3.4
python-multipart==0.0.9
httpx==0.27.2
# Optional for scraping (pip install playwright && playwright install chromium):
# playwright>=1.40.0
twilio>=9.0.0
openpyxl>=3.1.5
xlrd>=2.0.1
qrcode[pil]>=7.4.2
slowapi>=0.1.9
# <15: stripe-python 15.0.0 (2026-03-26) dropped dict inheritance from StripeObject,
# which breaks every obj.get(...) in the billing webhook and Stripe Connect code.
stripe>=7.0.0,<15
sentry-sdk[fastapi]>=2.0.0
supabase>=2.3.0
reportlab>=4.2.0
